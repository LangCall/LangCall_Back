spring:
  datasource:
    url: jdbc:mysql://localhost:3306/LangCall?serverTimezone=Asia/Seoul
    username: root
    password: root1234  # 설정한 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none   # 개발 중엔 update, 배포 땐 validate 또는 none 권장
    show-sql: true
    properties:
      hibernate:
        format_sql: true
